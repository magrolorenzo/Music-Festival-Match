kind = "web"
previewPath = "/"
title = "LivePulse"
version = "1.0.0"
id = "artifacts/livepulse"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20485

[services.development]
run = "pnpm --filter @workspace/livepulse run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/livepulse", "run", "build" ]
publicDir = "artifacts/livepulse/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20485"
BASE_PATH = "/"
